
    \documentclass{beamer}
    \beamertemplatenavigationsymbolsempty
    \usepackage{tikz}
    
    \begin{document}
    \centering
    \begin{tikzpicture}[scale=2.5]
    \foreach \Vt in {0,1,...,2}{
\draw[-latex] (0,0) -- ({cos(90+(1*360/2))},{sin(90+(1*360/2))});
\draw[] ({cos(90+(1*360/2))},{sin(90+(1*360/2))}) -- ({cos(90+((1+1)*360/2))},{sin(90+((1+1)*360/2))});}\draw[-latex] (0,0) -- ({cos(90+(2*360/2))},{sin(90+(2*360/2))});
\draw[] ({cos(90+(2*360/2))},{sin(90+(2*360/2))}) -- ({cos(90+((2+1)*360/2))},{sin(90+((2+1)*360/2))});}
    \draw[] (0,0) -- ({cos(90)},{sin(90)})
     -- ++({cos(90+((1)*360/2))},{sin(90+((1)*360/2))})
 -- ++({cos(90+((2)*360/2))},{sin(90+((2)*360/2))})
;

    \draw[white] (-1.5,-1.5) rectangle (1.5,1.5);
    \end{tikzpicture}
    \end{document}
    